<% environment.context_class.instance_eval { include Rails.application.routes.url_helpers } %>
<% environment.context_class.instance_eval { include ActionView::Helpers::UrlHelper } %>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <a class="navbar-brand" href="/">GeoBox</a>
  <div class="collapse navbar-collapse">
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
      <li class="nav-item active">
        <a ng-show="$ctrl.logged_in" class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
    </ul>
    <span class="nav-item active">
      <span ng-show="$ctrl.logged_in"><%= link_to '', new_document_path, {class: 'fa fa-plus btn'} %></span>
      <span ng-show="$ctrl.logged_in"><%= link_to 'Logout', logout_path, {method: :delete, class: 'btn'} %></span>
    </span>
  </div>
</nav>